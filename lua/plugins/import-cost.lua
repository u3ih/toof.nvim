return {
        'barrett-ruth/import-cost.nvim',
        build = 'sh install.sh yarn',
        enabled = false,
        -- if on windows
        -- build = 'pwsh install.ps1 yarn',
        config = true
}
